return {
  -- "gc" to comment visual regions/lines
  --
  -- `:checkhealth which-key` (open the + sign with `l`) somehow shows that gc and gb
  -- have conflicting keybindings. But this one has priority, so it's okay I guess.
  'numToStr/Comment.nvim',
  opts = {}
}

